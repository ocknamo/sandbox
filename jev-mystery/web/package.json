{
  "name": "jev-mystery-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "kanabun dev",
    "build": "rm -rf ../../docs/mystery && kanabun build index.html --outdir ../../docs/mystery --no-sourcemap && rm -f ../../docs/mystery/*.map",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kanabun/core": "^0.0.1",
    "@kanabun/router": "^0.0.1"
  },
  "devDependencies": {
    "@kanabun/cli": "^0.0.1",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
